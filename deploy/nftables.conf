#!/usr/sbin/nft -f

# Only the immutable fail-close base table is persisted. The dynamic table
# belongs exclusively to the Agent and is reconstructed from SQLite.
flush ruleset
include "/etc/nftables.d/pgw-base.nft"
