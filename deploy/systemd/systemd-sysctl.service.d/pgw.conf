[Unit]
# IPv4 forwarding from 99-pgw.conf is never applied unless the distro
# nftables loader and its pgw_base postcondition both succeeded.
Requires=nftables.service
After=nftables.service
Before=pgw-api.service pgw-agent.service
